#!/bin/bash

# no flags used
the file.the
the build file.the # equivalent to the line above
the run file.the

### Flags:

# Show use usage options
the -h
the --help

# Disabled color
the --no-color file.the

# Turn off warning reports
the --suppress-warnings file.the

# Treat warnings as errors
the --strict file.the